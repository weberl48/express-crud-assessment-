extends layout

block content
    a(href="/")
      div.header
        h1 The Zine
    div.content
        h3
          a(href="/articles/" + allArticles._id + "/edit") Edit
        h1=allArticles.title
        p=allArticles.body
        p=allArticles.excerpt
